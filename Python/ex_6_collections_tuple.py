# Collections - Tuple - ()
# - Same as Lists
# - Cannot Modify the elements of tuple
# Ordered, allows duplicates
# Tuple is iterable. Can be accessed using For-loop / Index

T=(1,2,3,4,5)
print(type(T))
print(len(T))

L=list(T)
print(L)